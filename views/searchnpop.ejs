<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PesquisaNpop</title>
    <link rel="stylesheet" href="/style.css">
    <script>
        function validarFormulario(event) {
            const nomePop = document.getElementById('nomePop').value.trim();

            if (!nomePop) {
                event.preventDefault(); // Impede o envio do formulário
                alert('Preencha o valor para pesquisar.');
            }
        }
    </script>
</head>
<body>
    <div class="container">
        <a href="/consultar" class="voltar">← Voltar</a>
        <h1>Digite o Nome Popular</h1>

        <form action="/pesquisanpop" method="POST" onsubmit="validarFormulario(event)">
            
            <input type="text" id="nomePop" name="nomePop">

            <button type="submit">Pesquisar</button>
        </form>
    </div>
</body>
</html>
